{
  "scripts": {
    "build": "npx pug -w index.pug & npx stylus -w main.styl",
    "serve": "npx browser-sync start -sw --no-notify",
    "develop": "tmux splitw 'npm run build' && tmux splitw 'npm run serve'"
  },
  "dependencies": {
    "browser-sync": "^2.26.3",
    "pug-cli": "^1.0.0-alpha6",
    "stylus": "^0.54.5"
  }
}
